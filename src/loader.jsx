import React from 'react';
import { allergens } from '../src/assets/allergens.png';

const Loader = () => {
 return (
     <div>
         <img scr={allergens} alt="allegern icons loading"/>
     </div>
 )
}

export default Loader;
